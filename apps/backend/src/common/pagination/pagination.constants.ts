/** Maximum allowed page size for list endpoints (enforced server-side). */
export const MAX_PAGE_SIZE = 100;

/** Default page size when the client omits `pageSize`. */
export const DEFAULT_PAGE_SIZE = 20;
