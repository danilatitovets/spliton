# DB migration strategy (read-only audit)

> Generated by `node --env-file=.env scripts/db-strategy-report.mjs`.  
> **Do not** run `prisma migrate resolve` or baseline on a populated DB without a written plan and backup.

## Current state (connected `DATABASE_URL`)

| Check | Result |
|-------|--------|
| `_prisma_migrations` | **MISSING** |
| Public tables | ~56 (legacy schema) |
| `users` rows | ~93 (treat as production/dev data — backup before destructive ops) |
| Local migration folders | **54** (`20260430211728_init_spliton_schema` … `20260624120000_production_gaps_foundation`) |
| Closeout tables | `notifications` EXISTS; `legal_policies`, `treasury_accounts`, `event_outbox`, `idempotency_records`, `referral_profiles`, `partner_profiles`, `system_announcements`, `deposit_address_pool` **MISSING** |

## Why `migrate deploy` returns P3005

Prisma Migrate expects either:

1. An **empty** database, or  
2. A database with **`_prisma_migrations`** tracking applied migrations.

This database has tables but **no** `_prisma_migrations` → Prisma refuses to apply `init` on a non-empty schema → **P3005**.

## Recommended path (safe)

### **Option A — PRIMARY: dedicated clean staging/e2e DB**

1. Create a **new** Supabase project (or empty Postgres).
2. Set in `.env` (or CI secrets):
   - `DATABASE_URL` — pooler `:6543?pgbouncer=true`
   - `DIRECT_URL` — `db.<project-ref>.supabase.co:5432` (not the pooler host)
   - `TEST_DATABASE_URL` / `TEST_DIRECT_URL` — same project or a second isolated project
3. `npx prisma migrate deploy --schema=prisma/schema.prisma`
4. `npm run prisma:seed` (legal policies, deposit pool, etc.)
5. Point e2e at `TEST_DATABASE_URL` via `apps/backend/test/jest-e2e.setup.ts`

### **Option C — backup → recreate staging**

1. Backup current staging/dev (Supabase dashboard or `pg_dump`).
2. Create empty DB / new project.
3. Option A steps 3–5.

### **Option B — baseline existing DB (only with explicit sign-off)**

Use only if:

- Schema already matches latest migration SQL (drift check by DBA).
- Stakeholders accept marking migrations as applied **without** re-running SQL.
- Written baseline report per migration (not automated in CI).

**Never** baseline the DB with ~93 users without backup and approval.

## E2E on legacy DB (current URL)

Until Option A is done, e2e uses `SKIP_SCHEMA_BOOTSTRAP=true` and disabled workers in `create-e2e-app.ts` so **AppModule boots**. HTTP flows that need closeout tables or successful registration will still fail until migrations + seed run on a clean DB.
