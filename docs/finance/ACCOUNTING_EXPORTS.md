# Accounting exports

User: `GET/POST /api/v1/accounting/statements/*` — очередь `generated_documents` с kinds:

- annual_income_statement, monthly_wallet_statement, trading_summary, …

**Disclaimer:** не налоговая/юридическая консультация — требует проверки бухгалтером.

Admin accounting exports — через existing admin reports + RBAC ACCOUNTANT.

See `docs/user/TAX_AND_STATEMENTS.md`
